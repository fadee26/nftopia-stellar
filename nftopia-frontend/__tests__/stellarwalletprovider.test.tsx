import React from "react";
import { render } from "@testing-library/react";

describe("StellarWalletProvider", () => {
  it("renders without crashing", () => {
    render(<div>StellarWalletProvider present</div>);
  });
});