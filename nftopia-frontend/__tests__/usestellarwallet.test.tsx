import React from "react";
import { render } from "@testing-library/react";
 
describe("useStellarWallet", () => {
  it("renders without crashing", () => {
    render(<div>useStellarWallet hook present</div>);
  });
});