import React from "react";
import { render } from "@testing-library/react";

describe("walletStore", () => {
  it("renders without crashing", () => {
    render(<div>walletStore present</div>);
  });
});