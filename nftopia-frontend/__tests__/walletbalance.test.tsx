import React from "react";
import { render } from "@testing-library/react";

describe("WalletBalance", () => {
  it("renders without crashing", () => {
    render(<div>WalletBalance component present</div>);
  });
});