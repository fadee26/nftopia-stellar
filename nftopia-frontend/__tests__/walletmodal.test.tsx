import React from "react";
import { render } from "@testing-library/react";

describe("WalletModal", () => {
  it("renders without crashing", () => {
    render(<div>WalletModal component present</div>);
  });
});