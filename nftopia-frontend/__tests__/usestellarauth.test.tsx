import React from "react";
import { render } from "@testing-library/react";

describe("useStellarAuth", () => {
  it("renders without crashing", () => {
    render(<div>useStellarAuth hook present</div>);
  });
});