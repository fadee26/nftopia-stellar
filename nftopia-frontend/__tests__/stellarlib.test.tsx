import React from "react";
import { render } from "@testing-library/react";

describe("stellar lib utilities", () => {
  it("renders without crashing", () => {
    render(<div>stellar lib utilities present</div>);
  });
});